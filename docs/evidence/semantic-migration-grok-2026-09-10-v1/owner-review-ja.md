# 意味マイグレーション実LLM検証 — 実行条件レビュー

2026-09-10 / revision 1 / 準備済み・有料実行は未承認・未実行。
実行ID: `semantic-migration-grok-2026-09-10-v1`。
準備証跡 a724ae39、実装スナップショット 502b5ea1。

## レビュー対象と概要

承認対象はこの文書の実行条件である。実装全体やADRの再承認ではない。
英語・JSONの参照資料に含まれる実行条件の日本語説明を本文にまとめている。
大容量のスキーマやソースコードは参照資料であり、今回のレビュー対象には含めない。
既存の承認済みB5プロンプトと検証処理は変更していない。

これまでのローカル検証では、実モデルが意味を保って移行できるかは分からない。
今回は合成キャラ1件で、APIが構造化出力スキーマを受理するか、
旧規範を適切な役割へ分けられるか、不整合があれば限定修正できるかを観測する。
過去の有料実行の承認を流用しない、新規の実行である。

## 実行条件（英語契約の日本語説明）

| 項目 | 条件 |
|---|---|
| API | xAI Chat Completions、https://api.x.ai/v1/chat/completions |
| モデル | grok-4.3固定。返却モデルの一致を検査 |
| 生成設定 | reasoning_effort=none、temperature=0、stream=false |
| 出力形式 | B5が生成するJSON Schemaをstrict=trueで送信。勝手に緩和しない |
| 件数 | 合成キャラ1件、1回の移行試行 |
| 最大要求数 | 初回生成・初回レビュー・修正と再レビュー2組、合計6要求 |
| 入力制限 | 各リクエストのシリアライズ済み本文180,000 UTF-8 bytesまで。スキーマを含む |
| 出力制限 | 各4,096 tokensまで |
| 予約 | 入力bytes＋2,048のフレーミング余裕＋最大出力。合計1,120,000 tokensまで |
| 費用上限 | 予約計算で1.50 USD。超える要求は送信しない |
| 単価 | 100万tokens当たり入力1.25 USD、出力2.50 USD。キャッシュ割引は見込まない |
| 待機 | 各要求90秒、通信の再試行・リダイレクト・fallback・ツール呼出しなし |
| 保存 | 合成入力、全要求、HTTP応答、usage、差分、候補、検証結果、隔離SQLite |
| 再実行 | live-startを排他作成。同じ実行の再送は禁止。結果不明は調査して別途判断 |
| 対象外 | 本番DB・既存ユーザーキャラ・現在世代更新・設定切替・デプロイ・B6の実装 |

単価と機能の出典は2026-09-10に確認した
[xAI公式Grok 4.3仕様](https://docs.x.ai/developers/models/grok-4.3)。
構造化出力APIの仕様は[xAI公式資料](https://docs.x.ai/developers/model-capabilities/text/structured-outputs)。
最大予約は1要求186,144 tokens、6要求で1,116,864 tokens / 1.4268 USDとなる。
ただしbytesからのtoken予約は保守的見積りであり、請求額を保証するものではない。
返却usageが予約を超えた場合は記録して停止する。200K以上の高コンテキスト料金帯を想定しない。

## 合成入力と期待する観測

「検証用アカリ」は、青い外套の架空の剣士で、能力は既存の既定値を使用する。
本番データも自然言語の追加ソースも使わない。

1. 優先度60の方針：「勝利だけに固執せず、相手との攻防を楽しみたい。」
   行動selectorはなく、既存の基本攻撃をfallbackとして持つ。
   方針は顕在意識のguidanceへ、fallbackは機械的fallbackへ意味を分けることを期待する。
2. 優先度40の実行可能な規範：「基本攻撃を優先したい。」
   既存の基本攻撃selectorを持つ。実行可能な選択条件を失わず、
   発話可能な方針の扱いと矛盾しないことを期待する。

能力・公開文・元の優先度・適用条件・由来を保持できるかを確認する。
モデルが選んだ操作・理由・元情報・移行先・退避値を全体レビューとともに残す。
生成とレビューは同じモデルの別要求で、レビューに過去の思考や会話履歴は渡さない。
意味修正はエラー箇所だけでなく必要な正常部分も対象にできるが、
統合後は候補全体を再検証する。

観測結果は、API受理、構造検証、意味保存、修正前後、要求数、usage、費用、経過時間に分けて報告する。
自然に修正が発生しなければ「修正能力は未観測」とし、成功したことにしない。
6種類すべての操作をこの1件で網羅できるとは主張しない。

## 停止条件と保全

送信前に、準備証跡と現在のソース・ビルド済み共有コード・契約・初回入力を照合する。
不一致、上限超過、保存失敗では送信しない。
通信失敗・HTTP失敗・モデル違い・usage不明は停止し、会計を0と推測しない。
切断やJSONパース不能は自動再送しない。raw応答と既知のusageを保全する。
構造または意味の不整合にはB5の最大2回の修正を使い、解消しなければレビュー必要として終了する。
モデルが不確実とした場合も、その不確実性を消さない。

新しい一時ディレクトリのSQLiteだけを使用し、環境変数や.envの本番DB設定を使用しない。
秘密値はsecdatから子プロセスへ注入し、記録に含めない。TLS検証を無効化した実行は拒否する。
移行前後の現在世代が同一であること、activated=falseを確認する。
SQLiteには合成データだけがあり、後日の調査用にローカル保持する。

## ローカル証拠と差分

前回B5に追加したのは、実験専用のHTTP境界、隔離ランナー、合成入力、スナップショット照合と9テスト。
既存B5の意味処理・通常のキャラ作成・戦闘経路は変更していない。

- 追加9テスト成功。全756テスト成功（339 / 389 / 20 / 3 / 5）。
- 全型検査、ビルド、jscpd、Lizard成功。既存Viteのchunkサイズ警告は残る。
- ネットワークなしで6要求・18イベント・6永続要求を確認。現在世代は不変。
- オフライン本文は126,165～142,427 bytes、予約費用合計1.0673225 USD。
  これは実課金ではなく、実モデルの後続応答によって本文量は変わる。
- 合成応答は意図的に未解決として2修正を通す試験用であり、モデル品質の証拠ではない。
- CLI LLMThink監査は準備方針、合成入力のRCA、上限見積り、準備完了判断でfatal/error/warning=0。
- secdatの注入dry-run成功。実APIでの認証・モデル利用可否は未確認。

証拠連鎖は C-PROBE-001（ローカル準備完了・高信頼）← E1～E4
（準備結果、検証コマンド、秘密注入メタデータ、実装読取り）である。
A1は準備と記録のみ実施。有料実行は未実施。

## 論点・対案・未確認事項

推奨は、この範囲で先に1件を観測すること。B6完成前にAPI・意味処理の問題を確認できる。
一方、1件・同一モデルのレビューでは一般的な品質や独立した正しさを証明できない。
比較対象の旧方式を走らせないため、単調化や全体品質の「改善」を判定する実験でもない。

対案は、B6まで待って受入・反映込みで検証すること。工程全体を確認できる反面、問題の発見が後になる。
複数キャラや強制的な不整合注入を追加する案は網羅性を増すが、今回は費用と範囲を増やさない。

実APIのスキーマ受理、自然発生する不整合、意味保存、出典の正当性はまだ不明。
既存ADR全件チェックの未解決と先行PostgreSQL試行のpublic影響不明も、この検証では解消しない。
この実行条件の承認は、本番反映、再試行、追加ケース、commit/pushの承認にはならない。
