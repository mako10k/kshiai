# 意味マイグレーション実LLM検証 v2 — 実行条件レビュー

2026-09-10 / revision 1 / ローカル準備済み・有料実行は未承認、未実行。
実行ID：`semantic-migration-grok-2026-09-10-v2`。
準備証跡：451cc1c5。実装スナップショット：d40eaef0。

## レビュー対象と目的

承認対象は、この文書に示す合成キャラ1件の実行条件全体。
英語・JSONの実行契約の日本語説明を、この文書に全て記載する。
参照先の大容量スキーマ・ソース・過去のADRは今回の承認対象ではない。
本書は実行条件の正本。生成・レビューの製品契約やADRの再承認ではない。

目的は、修正した出力スキーマが実APIに受理されるか、キャラの意味を保存して
V2からV3へ移行できるか、必要なら意味的な影響先を含む限定修正が働くかを観測すること。
オフライン試験だけでは、この3点の実モデル品質は判断できない。

## 前回v1との違い

- 前回は初回HTTP400で出力スキーマを拒否された。v1は消費済みとして保持する。
- 今回は新しい実行ID・隔離DB・準備証跡・保存先を使用。v1の承認を流用しない。
- プロンプト識別子は `character-semantic-migration-prompt-v2`。
- 任意JSONの出力定義を非循環の等価表現へ変換し、移行先V3仕様の5つの自己参照aliasを補正した。
- xAI送信前の共通循環検査を適用。strict指定、必須項目、操作種類、受信後の型・意味検証は維持する。
- 合成キャラ、モデル、生成設定、要求数・費用上限、再送禁止、本番除外は前回と同じ。
- この準備で変更した製品ソースはなく、実験ランナーと合成試行の識別子接続だけを変更した。
  前回ターンのスキーマ是正は今回の固定スナップショットに含まれる。

## 実行条件全文（日本語）

| 項目 | 条件 |
|---|---|
| API | xAI Chat Completions、https://api.x.ai/v1/chat/completions |
| モデル | grok-4.3固定。返却モデルの一致を確認 |
| 生成 | reasoning_effort=none、temperature=0、stream=false |
| 応答形式 | v2が生成したJSON Schemaをstrict=trueで送信。勝手に緩和しない |
| 対象 | 下記の合成キャラ1件、移行試行1回 |
| 要求数 | 生成1回、レビュー1回、修正・再レビュー最大2組、合計最大6要求 |
| 入力 | 本文全体（スキーマ込み）180,000 UTF-8 bytes／要求まで |
| 出力 | 4,096 tokens／要求まで |
| 予約 | 入力bytes＋2,048 tokensの余裕＋最大出力。全体1,120,000 tokensまで |
| 費用 | 予約計算で最大1.50 USD。上限を超える要求は送信しない |
| 単価 | 入力1.25 USD／100万tokens、出力2.50 USD／100万tokens。キャッシュ割引を見込まない |
| 時間 | 90秒／要求。最大6要求で通信待機は最大9分、別途ローカル保存・検証時間 |
| 禁止 | 通信再試行、リダイレクト、fallback、ツール呼出し、同一run再送 |
| 保存 | 合成入力、全要求・HTTP応答・usage、候補、差分、検証結果、隔離SQLite |
| 対象外 | 本番DB、実ユーザーキャラ、現在世代更新、設定activation、deployment、B6実装、commit/push |

単価とreasoning設定は今回も[xAI公式Grok 4.3仕様](https://docs.x.ai/developers/models/grok-4.3)で確認。
文法制約は[xAI構造化出力仕様](https://docs.x.ai/developers/model-capabilities/text/structured-outputs)を参照。
最大予約は1要求186,144 tokens、6要求1,116,864 tokens／1.4268 USD。
bytesを用いる予約は保守的な推計であり、請求額の保証ではない。
実usageが予約を超えれば記録して停止する。200K超の料金帯を想定しない。

## 合成入力と観測

「検証用アカリ」：青い外套の架空の剣士。既定の能力を持ち、本番データや追加の自然言語ソースは使わない。

1. 優先度60：「勝利だけに固執せず、相手との攻防を楽しみたい。」
   行動selectorなし、基本攻撃fallbackあり。
   方針を顕在意識のguidanceへ、fallbackを機械的fallbackへ分離することを期待する。
2. 優先度40：「基本攻撃を優先したい。」
   基本攻撃selectorあり。実行可能な条件を失わず、方針との矛盾を避けることを期待する。

能力・公開文・優先度・適用条件・由来・fallbackの保全を確認する。
操作・理由・元情報・移行先・退避値と、統合後の候補全体のレビューを保存する。
生成とレビューは同一モデルの別要求であり、過去の会話・思考履歴は渡さない。
修正対象はエラー箇所に限定せず、意味的整合性のために必要な正常部分まで許可する。
統合後は候補全体を再検証する。内部思考の開示は要求せず、契約上の短い理由と不確実性を記録する。

API受理、構造、意味保存、修正前後、要求数、usage、推計費用、経過時間を分けて報告する。
自然に修正が起きなければ修正能力は「未観測」。6操作の網羅や一般的品質を証明したとはしない。
旧方式との比較実験ではなく、セリフの単調化の改善を判定するものでもない。

## 停止・保全・再開

送信直前に準備証跡、現ソース、ビルド済み共有コード、実行契約、初回入力を照合する。
不一致、上限超過、保存失敗、TLS検証無効では送信しない。
新規一時ディレクトリのSQLiteだけを使い、環境変数/.envの本番DB設定を使用しない。
秘密値はメイン作業ツリーのsecdatドメインから注入し、証跡に含めない。

live-startを排他作成し、結果不明でも同じrunを再送しない。
通信・HTTP失敗、モデル不一致、usage不明では停止。会計を0と推測しない。
応答のJSON解析不能でも自動再送せず、raw応答と判明したusageを保持する。
構造・意味の不整合は既存の最大2回修正を使用し、未解決ならreview_requiredとして終了する。
現在世代が前後同一、activated=falseであることを確認する。
合成DBと証跡は後日の調査用にローカル保持し、勝手に削除しない。

## 確認済みの準備と未確認事項

E-PROBEV2-001：オフライン6要求・18イベント。ネットワーク0、現在世代不変、activated=false。
本文125,540〜141,457 bytes、予約合計1.06134125 USD。実課金ではない。
応答スキーマ6件と移行先スキーマ6件は共通循環検査を通過し、全てv2を指定。
合成応答は意図的に未解決のまま2回修正を通す試験用で、LLM品質の証拠ではない。

E-PROBEV2-002：全769テスト・型検査・ビルド・jscpd・Lizard成功。
既存のVite chunk-size警告は残る。
E-PROBEV2-003：秘密注入dry-run成功、TLS有効、実送信開始記録なし。
現ソースとビルド済み共有コードは準備スナップショットに一致。

C-PROBEV2-001（高信頼）：E-PROBEV2-001〜003によりローカル準備完了。
A-PROBEV2-001（検証目的・実行済み）：準備、ローカル試験、記録。
有料実行は保留。CLI監査はfatal/error/warning=0。
詳細は[準備判断](../semantic-probe-v2-preparation-2026-09-10.think)と
[準備完了判断](../semantic-probe-v2-readiness-2026-09-10.think)を参照。

## 論点・対案

推奨はこの1件を先に観測すること。B6の受入・反映機能へ進む前に実APIと意味処理を確認できる。
欠点は有料であり、1件・同一モデルのレビューだけでは独立した正しさや品質分布を証明できないこと。
対案はB6まで延期すること（工程全体を検証しやすいが、問題発見が遅れる）、
またはケース追加（網羅性は増すが費用と範囲が広がる）。今回は追加しない。

実APIの受理、自然発生の修正、意味保存、実コストは未確認。
既存ADR全件チェックの問題、先行PostgreSQL試行のpublic影響不明も、この実験では解消しない。
承認はこのv2準備証跡の1回に限り、追加実行・条件変更・本番反映・commit/pushには及ばない。
